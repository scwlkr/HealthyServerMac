#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ -x "${ROOT_DIR}/dist/buoy" ]]; then
  exec "${ROOT_DIR}/dist/buoy" "$@"
fi

printf 'Buoy is not built yet. Run ./scripts/build-cli.sh or ./install.sh first.\n' >&2
exit 1
